import 'package:flutter/material.dart';
import 'createMaterialColor.dart';

class Palette {
  static var materialPurple =
      createMaterialColor(const Color.fromARGB(255, 98, 81, 248));

  static var materialGreen =
      createMaterialColor(const Color.fromARGB(255, 0, 255, 122));

  static var materialBronze = (const Color.fromARGB(255, 255, 177, 129));

  static var materialBlueApp =
      createMaterialColor(const Color.fromARGB(255, 60, 89, 219));

  static var grey = Colors.grey;
}
